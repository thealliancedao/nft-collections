# aDAO — Metadata

Full per-token metadata for the AllianceDAO NFT collection (10,000 NFTs).

- `all_nfts_metadata.json` — name, description, image + thumbnail IPFS URLs, dna, id, date, and full attributes for every token.
